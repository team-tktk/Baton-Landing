# 기여 가이드

## 브랜치

| 브랜치 | 역할 | 배포 |
| --- | --- | --- |
| `main` | 운영. `dev`에서만 머지합니다 | S3 + CloudFront |
| `dev` | 통합 브랜치. 작업 브랜치가 모이는 곳입니다 | Vercel |
| 작업 브랜치 | `dev`에서 따서 `dev`로 PR을 올립니다 | 없음 |

`dev`에 머지되면 Vercel에 올라가 팀이 확인합니다. `main`에 머지되면 운영에 바로 나갑니다.

`main`과 `dev`에는 직접 커밋하지 않습니다. 항상 브랜치를 따서 PR로 올립니다.

### 브랜치 이름

```
<타입>/<이슈번호>-<요약>
```

```
feat/12-mobile-nav
fix/18-hero-squeeze
docs/23-breakpoint-guide
```

타입은 아래 커밋 타입과 같은 걸 씁니다. 요약은 영어 소문자와 하이픈으로 짧게 적습니다.

## 커밋

[Conventional Commits](https://www.conventionalcommits.org/ko/v1.0.0/)를 씁니다.

```
<타입>(<범위>): <요약>

<본문>
```

| 타입 | 언제 |
| --- | --- |
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `docs` | 문서만 고쳤을 때 |
| `style` | 포맷, 세미콜론 등 동작이 그대로일 때 |
| `refactor` | 동작은 그대로고 구조만 바꿀 때 |
| `perf` | 성능 개선 |
| `test` | 테스트 추가나 수정 |
| `chore` | 의존성, 설정, 잡일 |
| `ci` | CI 설정 |

- 범위는 생략할 수 있습니다. 보통 컴포넌트나 파일 이름을 씁니다. 예: `feat(nav):`
- 요약은 한국어 명령형으로 쓰고 마침표를 찍지 않습니다. 50자 안쪽이면 좋습니다.
- 왜 바꿨는지는 본문에 적습니다. 무엇을 바꿨는지는 diff가 이미 말해줍니다.

```
feat(nav): 900px 이하 모바일 메뉴 추가

좁은 폭에서 섹션 링크가 사라져 이동할 방법이 없었습니다.
닫힌 패널에는 inert를 걸어 탭 순서에서 빠지게 했습니다.
```

## 작업 흐름

1. **이슈를 먼저 만듭니다.** 무엇을 왜 하는지 여기에 남깁니다.
2. `dev`를 최신으로 맞추고 브랜치를 땁니다.
   ```bash
   git switch dev && git pull && git switch -c feat/12-mobile-nav
   ```
3. 작업하고 커밋합니다.
4. `dev`로 PR을 올리고 본문에 `Closes #12`로 이슈를 겁니다.
5. CI가 통과하고 리뷰어 1명이 승인하면 머지합니다.
6. **Squash merge**로 머지합니다. PR 제목이 그대로 커밋이 되므로 PR 제목도 커밋 규칙을 따릅니다.

`dev`가 충분히 쌓이면 `dev` → `main` PR을 올립니다. 이 PR이 머지되는 순간 운영에 배포되므로, Vercel에 올라간 `dev`를 먼저 확인하고 올려주세요.

배포 구성과 등록해야 하는 Secrets는 [README의 배포 항목](README.md#배포)에 있습니다.

## 코드 규칙

[README](README.md)의 스타일 규칙과 반응형 항목을 먼저 읽어주세요. 특히 두 가지를 자주 놓칩니다.

- 색은 컴포넌트에 hex로 쓰지 않고 `src/styles/tokens.css`의 토큰을 씁니다.
- 브레이크포인트는 네 단(1000 / 900 / 720 / 560)뿐입니다. 새 값을 만들지 않습니다.
