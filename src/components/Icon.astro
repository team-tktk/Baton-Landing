---
import { iconPaths, type IconName } from './icons'

interface Props {
  name: IconName
  class?: string
}

const { name, class: className } = Astro.props
---

<svg aria-hidden="true" class={className} viewBox="0 0 24 24" set:html={iconPaths[name]} />
